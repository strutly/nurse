.tag-active{
  background-color: var(--white);
  color:var(--main);
}

page{
  background-color: var(--white);
}
.cu-list.menu>.cu-item{
  padding: 30rpx;
}
.cu-form-group textarea{
  padding: 20rpx;
  min-height: 200rpx;
}
.footer{
  position: fixed;
  bottom: 20rpx;
  left:0;
  right:0;
}

.text-xxl{
  font-size: 44rpx;
}
.safe-btns{
  padding-bottom: calc(250rpx + constant(safe-area-inset-bottom) / 2);
  padding-bottom: calc(250rpx + env(safe-area-inset-bottom) / 2);
}